{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon --watch src --exec node -r dotenv/config src/index.js",
    "seed": "node -r dotenv/config seed.js",
    "start": "node src/index.js",
    "test": "cross-env NODE_ENV=test DATABASE_URL=file:./test.db npx prisma migrate reset --force && jest --runInBand"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "razorpay": "^2.7.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.4.2",
    "nodemon": "^2.0.20",
    "prisma": "^5.0.0",
    "supertest": "^7.2.2"
  }
}
